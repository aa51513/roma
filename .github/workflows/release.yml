name: release
on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  linux-x86_64:
    name: Linux x86_64
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-unknown-linux-gnu
          override: true
      - run: cargo build --all-features --release && mkdir -p roma-linux-x86_64
                                                  && mv target/release/romac ./roma-linux-x86_64/
                                                  && mv target/release/romad ./roma-linux-x86_64/
                                                  && tar zcf roma-linux-x86_64.tar.gz ./roma-linux-x86_64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-linux-x86_64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  linux-musl-x86_64:
    name: Linux musl x86_64
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-unknown-linux-musl
          override: true
      - run: sudo apt-get -y install musl-tools && rustup target add x86_64-unknown-linux-musl && cargo build --all-features --release --target x86_64-unknown-linux-musl
                                                         && mkdir -p roma-linux-musl-x86_64
                                                         && mv target/x86_64-unknown-linux-musl/release/romac ./roma-linux-musl-x86_64/
                                                         && mv target/x86_64-unknown-linux-musl/release/romad ./roma-linux-musl-x86_64/
                                                         && tar zcf roma-linux-musl-x86_64.tar.gz ./roma-linux-musl-x86_64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-linux-musl-x86_64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  windows-x86_64:
    name: Windows x86_64
    runs-on: windows-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-pc-windows-msvc
          override: true
      - run: cargo build --all-features --release && mkdir -p roma-windows-x86_64
                                                  && mv target/release/romac.exe ./roma-windows-x86_64/
                                                  && mv target/release/romad.exe ./roma-windows-x86_64/
                                                  && 7z a roma-windows-x86_64.zip ./roma-windows-x86_64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-windows-x86_64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  darwin-x86_64:
    name: Darwin x86_64
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: x86_64-apple-darwin
          override: true
      - run: rustup target add x86_64-apple-darwin && cargo build --all-features --release --target x86_64-apple-darwin
                                                   && mkdir -p roma-darwin-x86_64
                                                   && mv target/x86_64-apple-darwin/release/romac ./roma-darwin-x86_64/
                                                   && mv target/x86_64-apple-darwin/release/romad ./roma-darwin-x86_64/
                                                   && zip roma-darwin-x86_64.zip ./roma-darwin-x86_64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-darwin-x86_64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
            
  darwin-aarch64:
    name: Darwin Aarch64
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: aarch64-apple-darwin
          override: true
      - run: rustup target add aarch64-apple-darwin && cargo build --all-features --release --target aarch64-apple-darwin
                                                    && mkdir -p roma-darwin-aarch64
                                                    && mv target/aarch64-apple-darwin/release/romac ./roma-darwin-aarch64/
                                                    && mv target/aarch64-apple-darwin/release/romad ./roma-darwin-aarch64/
                                                    && zip roma-darwin-aarch64.zip ./roma-darwin-aarch64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-darwin-aarch64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  linux-armv7:
    name: Linux ARMv7
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: armv7-unknown-linux-gnueabihf
          override: true
      - run: rustup target add armv7-unknown-linux-gnueabihf && cargo install cross --git https://github.com/cross-rs/cross
                                                             && cross build --all-features --release --target armv7-unknown-linux-gnueabihf
                                                             && mkdir -p roma-linux-armv7
                                                             && mv target/armv7-unknown-linux-gnueabihf/release/romac ./roma-linux-armv7/
                                                             && mv target/armv7-unknown-linux-gnueabihf/release/romad ./roma-linux-armv7/
                                                             && tar zcf roma-linux-armv7.tar.gz ./roma-linux-armv7/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-linux-armv7.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  linux-aarch64:
    name: Linux Aarch64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: aarch64-unknown-linux-gnu
          override: true
      - run: rustup target add aarch64-unknown-linux-gnu && cargo install cross --git https://github.com/cross-rs/cross
                                                         && cross build --all-features --release --target aarch64-unknown-linux-gnu
                                                         && mkdir -p roma-linux-aarch64
                                                         && mv target/aarch64-unknown-linux-gnu/release/romac ./roma-linux-aarch64/
                                                         && mv target/aarch64-unknown-linux-gnu/release/romad ./roma-linux-aarch64/
                                                         && tar zcf roma-linux-aarch64.tar.gz ./roma-linux-aarch64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-linux-aarch64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
  linux-musl-aarch64:
    name: Linux musl Aarch64
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: aarch64-unknown-linux-musl
          override: true
      - run: sudo apt-get -y install musl-tools && rustup target add aarch64-unknown-linux-musl
      - run: cargo install cross --git https://github.com/cross-rs/cross
      - run: cross build --all-features --release --target aarch64-unknown-linux-musl
      - run: mkdir -p roma-linux-musl-aarch64
      - run: mv target/aarch64-unknown-linux-musl/release/romac ./roma-linux-musl-aarch64/
      - run: mv target/aarch64-unknown-linux-musl/release/romad ./roma-linux-musl-aarch64/
      - run: tar zcf roma-linux-musl-aarch64.tar.gz ./roma-linux-musl-aarch64/*
      - name: Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            roma-linux-musl-aarch64.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

